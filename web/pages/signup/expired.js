import { Typography } from '@mui/material'

const ExpiredToken = () => {
  return (
    <div>
      <Typography textAlign="center" variant="h5">
        The link that we sent you in the email has expired. Sign up again!
      </Typography>
    </div>
  )
}

export default ExpiredToken
